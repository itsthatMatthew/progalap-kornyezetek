{
  "name": "ProgAlap - Dev Container",
  "image": "mcr.microsoft.com/vscode/devcontainers/base:ubuntu",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cpptools-extension-pack",
      ]
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y gcc g++ gdb && sudo apt-get clean && sudo rm -rf /var/lib/apt/lists/*"
}